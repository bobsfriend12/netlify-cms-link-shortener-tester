<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta http-equiv="X-UA-Compatible" content="IE=edge" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		{% if link %}
		<meta http-equiv="refresh" content="5;{{ link }}" />
		{% endif %}

		<link
			rel="shortcut icon"
			href="{{ siteinfo.faviconurl }}"
			type="image/x-icon"
		/>

		<title>{{ env.SITE_NAME }}</title>

		<style>
			.redirect {
				display: flex;
				flex-direction: column;
				align-items: center;
				justify-content: center;

				min-height: 100vh;

				text-align: center;
				font-family: sans-serif;
				font-size: x-large;
			}
		</style>
	</head>
	<body>
		<div class="redirect">
			<img
				src="{{ env.LARGE_LOGO_URL }}"
				alt="{{ env.SITE_NAME }} logo"
				class="redirect__logo"
			/>
			<p class="redirecting__text">Redirecting in 5 seconds...</p>
			<p class="redirect__fail">
				If you aren't redirected click
				<a href="{{ link }}" class="redirect__fail__link">here</a>.
			</p>
		</div>
	</body>
</html>
